import numpy as np 
import math

N = 5
M = 12
st = 0
A = np.zeros((N, M))

if st == 0:
  for i in st,N
  for j in st,M
  print(math.sin(N · (i+1) + M · (j + 1)))
else:
  for i in st,N
  for j in st,M
  print(math.sin(N · i + M · j))


